// import { selectAmenitiesListPageDomain } from '../selectors';

describe('selectAmenitiesListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
