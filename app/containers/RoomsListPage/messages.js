/*
 * RoomsListPage Messages
 *
 * This contains all the text for the RoomsListPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RoomsListPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RoomsListPage container!',
  },
});
