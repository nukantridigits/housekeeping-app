// import { selectRoomsListPageDomain } from '../selectors';

describe('selectRoomsListPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
