// import { selectAmenitiesPageDomain } from '../selectors';

describe('selectAmenitiesPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
